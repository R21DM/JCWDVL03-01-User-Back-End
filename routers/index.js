const registerRouter = require("./registerRouters");
const userRouters = require("./userRouters");
const productRouters = require("./productRouters");

module.exports = { registerRouter, userRouters, productRouters };
